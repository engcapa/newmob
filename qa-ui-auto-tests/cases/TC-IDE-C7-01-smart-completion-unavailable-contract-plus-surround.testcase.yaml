id: TC-IDE-C7-01
title: "smart completion unavailable contract plus surround"
description: |
  §8.18.10 gate case. Smart action visible but typed-unavailable without expected-type capability; surround try/catch on whole-line selection with one undo.
  Requires the Tauri native harness (and for provider cases the versioned
  jdtls fixture from __fixtures__/jdtls). Not executable in browser mode;
  recorded here so the catalog tracks the gate and the manifest can reference
  run results per platform.
covers: [F25.9]
tags: [code-workspace, ide-c7, native-gate, p1]
modes: [native]
fixtures: [reset_db]
timeout_sec: 300
steps:
  - open: "${cfg.app.base_url}"
  - wait_for: "[data-testid=\"tab-bar\"]"
  - click: "[data-testid=\"sidebar-tool-code-workspace\"]"
  - wait_for: "[data-testid=\"code-workspace-tab\"]"
  - screenshot: tc-ide-c7-01-placeholder.png
