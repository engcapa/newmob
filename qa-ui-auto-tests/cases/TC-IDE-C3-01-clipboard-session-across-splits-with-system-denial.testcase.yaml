id: TC-IDE-C3-01
title: "clipboard session across splits with system denial"
description: |
  §8.18.10 gate case. rectangular copy in split A, multi-caret paste in split C, undo restores all, system clipboard denied keeps workspace session.
  Requires the Tauri native harness (and for provider cases the versioned
  jdtls fixture from __fixtures__/jdtls). Not executable in browser mode;
  recorded here so the catalog tracks the gate and the manifest can reference
  run results per platform.
covers: [F25.7]
tags: [code-workspace, ide-c3, native-gate, p1]
modes: [native]
fixtures: [reset_db]
timeout_sec: 300
steps:
  - open: "${cfg.app.base_url}"
  - wait_for: "[data-testid=\"tab-bar\"]"
  - click: "[data-testid=\"sidebar-tool-code-workspace\"]"
  - wait_for: "[data-testid=\"code-workspace-tab\"]"
  - screenshot: tc-ide-c3-01-placeholder.png
