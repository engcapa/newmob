id: TC-033
title: Tunnel manager toggles autostart and credential reveal per row
covers: [F8.2]
tags: [autostart, credentials, legacy-imported, needs-review, p1, tunnel]
modes: [browser]
fixtures: [reset_db, ssh_required]
steps:
  - open: "${cfg.app.base_url}"
  - click: "[data-testid=\"ribbon-tunneling\"]"
  - wait_for: "text=\"New SSH tunnel\""
  - click: "button:has-text(\"New SSH tunnel\")"
  - wait: 1
  - wait_for: "input[placeholder=\"e.g. postgres-replica\"]"
  - fill: {selector: "input[placeholder=\"e.g. postgres-replica\"]", value: qa-ui-auto-tunnel-autostart}
  - fill: {selector: "input[placeholder=\"ssh.example.com\"]", value: "${cfg.ssh.host}"}
  - fill: {selector: "input[placeholder=\"user\"]", value: "${cfg.ssh.user}"}
  - fill: {selector: "input[placeholder=\"22\"]", value: "${cfg.ssh.port}"}
  # _TODO_MIGRATE: legacy step could not be auto-converted; please fix:
  # async page => { const inputs = page.locator(`input[placeholder="0"]`); if (await inputs.count() > 0) await inputs.first().fill("19181"); const dest = page.locator(`input[placeholder="db.internal"], input[placeholder="0.0.0.0"]`).first(); if
  - eval_readonly:
      expression: 'true'
      contains: 'true'
  # _TODO_MIGRATE: legacy step could not be auto-converted; please fix:
  # async page => { await page.locator(`button:has-text("Save")`).first().click({ force: true }); }
  - eval_readonly:
      expression: 'true'
      contains: 'true'
  - wait: 1
  - wait_for: "text=\"qa-ui-auto-tunnel-autostart\""
  # _TODO_MIGRATE: legacy step could not be auto-converted; please fix:
  # async page => { const row = page.locator(`tr:has-text("qa-ui-auto-tunnel-autostart")`); const before = await row.locator(`button[title*="Auto-start" i], button[title*="auto-start" i]`).first().getAttribute("title"); await row.locator(`butto
  - eval_readonly:
      expression: 'true'
      contains: 'true'
  # _TODO_MIGRATE: legacy step could not be auto-converted; please fix:
  # async page => { const row = page.locator(`tr:has-text("qa-ui-auto-tunnel-autostart")`); await row.locator(`button[title*="Show credentials" i], button[title*="Hide credentials" i]`).first().click(); }
  - eval_readonly:
      expression: 'true'
      contains: 'true'
  # _TODO_MIGRATE: legacy step could not be auto-converted; please fix:
  # async page => { const row = page.locator(`tr:has-text("qa-ui-auto-tunnel-autostart")`); await row.locator(`button[title="Test SSH connection"]`).first().click(); }
  - eval_readonly:
      expression: 'true'
      contains: 'true'
  - wait: 2
  - seed_dialog: {confirm: true}
  - wait: 1
  - screenshot: 033-tunnel-autostart-credentials.png
