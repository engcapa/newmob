<!-- Windows side-by-side manifest embedded into every linked target of this
     crate by build.rs (configure_windows_manifest), which documents why we own
     this instead of tauri-build.

     The Common-Controls v6 dependency mirrors tauri-build's default manifest;
     keep it in sync when upgrading tauri-build. The trustInfo block is what
     mt.exe synthesizes by default, declared explicitly here so the embedded
     manifest matches this file instead of being generated. asInvoker is
     correct: the elevated SocksCap helper is launched with
     "Start-Process -Verb RunAs" (src/sockscap/helper.rs), not via manifest
     elevation.

     ASCII only: mt.exe fails to parse this file if it contains non-ASCII bytes. -->
<assembly xmlns="urn:schemas-microsoft-com:asm.v1" manifestVersion="1.0">
  <dependency>
    <dependentAssembly>
      <assemblyIdentity
        type="win32"
        name="Microsoft.Windows.Common-Controls"
        version="6.0.0.0"
        processorArchitecture="*"
        publicKeyToken="6595b64144ccf1df"
        language="*"
      />
    </dependentAssembly>
  </dependency>
  <trustInfo xmlns="urn:schemas-microsoft-com:asm.v3">
    <security>
      <requestedPrivileges>
        <requestedExecutionLevel level="asInvoker" uiAccess="false" />
      </requestedPrivileges>
    </security>
  </trustInfo>
</assembly>
