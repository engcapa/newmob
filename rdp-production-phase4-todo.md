# macOS RDP 生产化阶段 4 待办

> 状态：待办，阶段 1～3 完成后保留；本轮不实现。
>
> 目标：对 macOS 上的 RDP client 与 RDP server 做真实环境发布认证。阶段 1～3 完成的是代码侧安全、完整性和生命周期基线；只有本清单的发布门禁全部通过后，才将二者标记为“生产可用”。

## 0. Win11 -> macOS 激活断开专项（2026-08-10）

代码分析与兼容策略修复见 [rdp-server-win11-macos-activation-regression.md](claudedocs/rdp-server-win11-macos-activation-regression.md)。以下项目必须在 macOS + Windows 11 真机完成：

- [ ] 默认 bitmap + 服务端捕获尺寸路径下，审批弹窗分别停留 1/10/25 秒后允许，均可进入桌面。
- [ ] 审批前桌面显示不被阻塞且输入无效；审批后键盘、鼠标和滚轮生效。
- [ ] 单独开启 `TAOMNI_RDP_EXPERIMENTAL_AVC420=1` 做 A/B，保存首帧、EGFX ACK/decoded progress 和断开错误链。
- [ ] 单独开启 `TAOMNI_RDP_EXPERIMENTAL_CLIENT_SIZE=1` 做 A/B，保存请求尺寸、ScreenCaptureKit resize 结果和首帧尺寸。
- [ ] 覆盖拒绝、30 秒超时、断开后旧审批失效以及再次连接。

## 1. 真实互操作矩阵

### RDP client

- 在项目声明支持的最低 macOS 版本、当前版本和前一大版本上运行验证。
- 覆盖 Windows 10/11、仍在支持期的 Windows Server，以及一套 xrdp 环境。
- 覆盖直连、HTTP 代理、SOCKS5、RD Gateway；RD Gateway 必须包含真实 IIS/NPS 环境，不能只依赖协议单测。
- 分别验证系统信任链、显式 SHA-256 证书指纹和 TOFU；检查证书轮换、主机名错误、过期证书及中间人证书均会按策略失败。
- 覆盖 NLA、域账户、本地账户、Unicode/IME、键盘布局、Retina/缩放、动态分辨率、音频、文本与文件剪贴板、只读/可写磁盘映射。

### RDP server

- 使用 Windows 自带 RDP 客户端、FreeRDP 和一套 macOS RDP 客户端交叉验证。
- 覆盖单显示器、多显示器、Retina、显示器热插拔、分辨率/缩放变化、全屏视频和静态桌面。
- 覆盖 view-only、本机控制确认、显式无人值守控制三种模式；验证单客户端策略、30 秒审批超时和断开后的旧审批失效。
- 覆盖错误凭据、并发连接、半开连接、恶意探测、超大剪贴板和重定向文件越界请求。

每个组合记录客户端/服务端版本、结果、已知限制和复现材料。P0/P1 缺陷必须清零；允许发布的 P2 缺陷必须有用户可见规避说明。

## 2. 长稳、故障注入与资源回收

- client 和 server 各完成至少一次 8 小时交互 soak 与 24 小时静置 soak。
- 完成至少 100 次连接/断开循环；线程、文件描述符、临时目录、剪贴板轮询器和内存不得持续增长。
- 注入网络断开、丢包、延迟、代理重启、RD Gateway 重启、Mac 睡眠/唤醒、锁屏/解锁和应用退出。
- 在审批弹窗、NLA、TLS、显示捕获和文件传输的每个阶段执行取消，确认有界超时且无残留会话。
- 撤销屏幕录制或辅助功能权限后必须安全失败；重新授权后的恢复路径需要实机验证。

## 3. 性能与画面质量预算

- 建立 1080p、Retina 2x 和 4K 的基准，记录帧率、端到端输入延迟、CPU、内存及上下行带宽的 p50/p95。
- 静态桌面验证近零更新；滚动、窗口拖动和视频播放验证脏区、帧节流及慢客户端背压。
- 评估当前 macOS 持久显示流是否满足功耗、热插拔和长期稳定性要求；若不能，迁移到直接的 ScreenCaptureKit 后端。
- 为性能回归设置硬门限并纳入发布 CI；门限以基准硬件实测结果确定并记录，不能只比较单元测试耗时。

## 4. macOS 发布与权限体验

- 校验证书签名、notarization、Hardened Runtime、沙箱/entitlement 和安装包升级路径。
- 首次启动、权限缺失、权限被撤销和系统设置返回后的提示必须可操作，且不会造成启动循环或静默黑屏。
- 验证辅助功能输入权限与屏幕录制权限的最小化请求：view-only 不应要求输入权限，未启动 server 不应提前弹权限。
- 验证多用户切换、Fast User Switching、锁屏和退出登录时的行为，并把 macOS 无法支持的登录屏/独立 GUI 会话明确写入发布说明。

## 5. 安全审计与模糊测试

- 完成 TLS/NLA、凭据保管、WebSocket session token/Origin、RD Gateway 认证、证书 pin/TOFU 数据库的威胁模型复核。
- 对本地 WebSocket 控制帧、RDP 文件重定向路径、CLIPRDR 数据和 RD Gateway/NDR 解码入口执行 fuzz/property 测试。
- 复核符号链接、路径穿越、TOCTOU、配额绕过、临时文件权限和会话间数据残留。
- 执行依赖漏洞与许可证审计；对 IronRDP server alpha 依赖记录版本冻结、升级验证和回滚策略。
- 日志与诊断包不得包含密码、vault 内容、剪贴板正文、文件内容、session token 或完整认证报文。

## 6. 可观测性与支持材料

- 为连接阶段、TLS/NLA、通道协商、重试、审批、捕获权限和断开原因建立稳定错误码与结构化诊断摘要。
- 提供用户可导出的脱敏诊断包，并记录 client/server 角色、传输类型、协商能力和资源统计。
- 补齐管理员部署说明、受支持矩阵、安全默认值、无人值守风险、证书轮换、网络暴露和故障排查文档。

## 7. 自动化与发布门禁

- 将现有 ignored live tests 接入隔离的真实 Windows/RD Gateway 测试环境，凭据只来自 CI secret store。
- 增加 RDP server 的真实客户端冒烟和连接生命周期自动化；浏览器 stub 测试不能替代 native 验收。
- 发布候选必须同时通过：前端全量测试与构建、Rust 全量单元/集成测试、互操作矩阵、soak、故障注入、性能门限、安全审计、签名/notarization。
- 所有门禁结果关联到具体 commit 和构建产物；任一安全失败、数据越界、无限重试、资源持续泄漏或不可恢复黑屏均阻止发布。

## 完成定义

阶段 4 只有在以下条件同时满足后才可关闭：

1. 两个角色的真实互操作矩阵均通过，已知限制已文档化。
2. 长稳和故障注入没有未解决的资源泄漏、死锁或无界等待。
3. 性能预算在支持的 Mac 硬件档位上达标。
4. 安全审计与关键解析入口 fuzz 无未修复高危问题。
5. 已签名、notarized 的发布候选在干净机器完成安装、升级、权限和回滚验收。
